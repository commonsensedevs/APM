//Create IProduct interface with product properties
 export interface IProduct {
        id: number;
        name: string;
        code: string;
        available: Date;
        price: number;
        rating: number;
    }
